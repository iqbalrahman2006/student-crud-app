{
  "name": "student-crud-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npm start --prefix server",
    "dev": "kill-port 3000 5000 && concurrently \"npm run client\" \"npm run server\"",
    "start": "npm run dev",
    "install": "npm install --prefix client && npm install --prefix server"
  },
  "dependencies": {
    "concurrently": "^6.5.1"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}